# Design QA：macOS 14 兼容与 macOS 26 Liquid Glass 设置页

[English](design-qa.en.md)

## 审查范围

- 设置窗口：默认及最小尺寸 1020×772，可自由缩放；
- 首次使用 Onboarding：约 1020×772，不显示步骤编号、总数、百分比、“跳过”或“稍后继续”；
- 页面：连接与语音、按键映射、统计、回眸、设置（含权限与隐私、通用、诊断与日志）；
- 各页面顶部仅显示主标题，不显示重复说明页面内容的副标题；
- 仓库截图：
  - [连接与语音](Screenshots/connection-and-voice.png)
  - [按键映射](Screenshots/key-mapping.png)
  - [设置（权限与隐私）](Screenshots/permissions-and-privacy.png)

## 当前实现

- 未完成当前流程版本时，主窗口使用 Typeless 式双栏 Onboarding：顶部仅显示“准备 › 设置 › 试一下”，实体遥控器路径九页、无遥控器路径十页，均在 `1020 × 772` 内一次完整展示、不使用内部滚动；先并列询问“有遥控器 / 当前没有遥控器”，仅无遥控器时下一页选择推荐 iPhone App 或“没有 iPhone 时”使用网页版；左侧完成当前任务，右侧显示对应控制方式、实时检查或输入法四项设置；音频页只平铺并接受 MiRemoteV 2ch 与 BlackHole 2ch，不使用下拉框；必要能力未通过时主按钮禁用，完成前根视图不显示设置页；
- Onboarding 当前卡点在页面内只突出一个主要修复动作，诊断复制为同卡片中的次要操作；完成页环境变化时直接返回受影响阶段，不新增 Sheet、Popover 或连续确认弹窗；
- 实体遥控器连接页在等待普通按键时必须直接写明“圆盘中间确定键或方向键”，并明确排除麦克风/语音键；检测到误按语音键时在原状态卡位置显示橙色纠正状态，不新增弹窗、不放宽普通按键门槛，并保留生产 HID 监听状态；
- 输入法设置右栏的系统 Fn 第 3 项使用匹配浅色/深色的 macOS 键盘设置原图，并继续显示实时冲突状态；Onboarding 首次设置统一固定 Fn/地球键，不显示 Command 选择器；完成后主设置页的 Command 模式必须显示第三方配置无法自动验证的持续提醒；截图和状态卡必须在默认窗口内同时可见；
- Onboarding 语音测试输入框页必须在同一张不可忽略的配置卡中同时显示无线麦发送语音键、SayAll 音频输出、第三方语音识别键和第三方麦克风；豆包额外显示“全局唤起语音”，Typeless 显示 Fn 点按语义，其他工具显示“需支持语音输入并自行配置麦克风”。第三方内部状态只能由用户逐项确认，不能伪装成自动检测；音频输出未就绪必须使用红色状态，切换工具或设备后确认自动失效，未确认不得继续；中文字号不低于 12pt。
- 设置页使用窄侧栏、标题区和分层内容区，五个页面的主要操作在最小窗口下可访问；页面可以滚动但不显示滚动条；
- 侧栏选择态和按键选择态使用低透明度语义蓝交互玻璃；
- 使用系统字体、语义字号和系统颜色，跟随浅色、深色、降低透明度与增强对比度设置；
- 窗口保留原生红黄绿按钮与逻辑标题，隐藏可见标题和标题栏分隔线，页面背景延伸到顶部；仅顶部专用空白区域可拖动窗口，页面内容、滑块和其他交互区域不会带动整个窗口移动，交互内容仍避开窗口按钮；
- macOS 26 的面板和按钮使用原生 `glassEffect` 与 glass button style；macOS 14/15 使用系统 Material 与标准按钮，不使用自定义模糊实现；
- 按键页面复用 `Resources/RC003-remote-photo.png`，保持 508×1030 原始比例；
- 普通实体按键按下时会高亮遥控器示意图并定位映射行，语音键使用独立的语音活动状态；
- 界面未展示实物上不存在的独立静音键。
- 普通界面使用产品语言，不显示遥控器型号代号、蓝牙语音协议名、按键协议名、十六进制按键编号或设备标识术语；
- 遥控器设备卡只用标准电池图标和百分比表达电量；空电、低电、正常和满电使用不同电池符号与语义颜色，充电或外接电源时在图标上叠加闪电，不再额外显示“可充电电池”文字；
- 统计页已改名为“个人中心 / Profile”，以已批准设计稿 `dist/designs/profile-v4/profile-v4-dark-refined.png` 为唯一视觉基线：宽窗口顶部四项 KPI 必须保持单行；窄窗口允许自适应为两列；下方左侧为排行榜、右侧为最近半年日历热力图，移除柱状图与“少—多”图例；日历方格必须为正方形并使用语义蓝色；排行和日历日期支持进入“回眸”；
- “回眸”作为紧跟“统计”的独立侧边栏页面，页头标题右侧显示最多两行的本地隐私说明，保存开关固定在最右侧，页面底部不重复隐私卡；页面默认展示全部 App 的倒序时间线，顶部应用栏固定显示“全部应用”，其余 App 按最近使用排序并支持单行横向浏览或在页面内展开为自适应网格，图标无法解析时回退通用 App 图标；日期分组默认展开最新一天，并支持逐日展开或折叠；本地 Agent 访问区放在历史之后，使用页面内开关、文本框、复制按钮和授权列表完成配置，不使用下拉框、Popover 或 Sheet；关闭时仍可查看和撤销授权；功能说明和无描边“全部删除”位于页面最底部；
- 网页连接邀请码弹窗顶部重点推荐 iOS App，TestFlight 公测地址不直接显示，并提供打开页面和复制链接操作；
- 设置页把当前版本、可更新版本、检查更新、更新内容与默认关闭的预发布更新开关放在同一区域，待更新版本 Release Notes 显示在其右侧；权限与隐私位于通用上方，诊断与日志同页展示；导出/导入配置同一行，登录自动启动/启动打开主窗口同一行；语言选项始终完整展示，不使用下拉框；官网和 GitHub 保持显眼，版本历史按钮、术语表与退出按钮不再显示；
- 设置页使用确认设计稿的紧凑平铺分区，不在首屏展示品牌 Hero、反馈大卡或展开二维码；支持入口位于诊断之后，分享模块固定为设置页最后一个模块，侧边栏“分享”会定位并展开该底部模块；
- 所有界面文案使用稳定语义 key；当前语言缺少 Markdown 帮助时回退英文。

## 交互、布局与字体硬性规范

- 用户悬停在开关、按钮或功能入口上时，可以通过系统帮助标签或同类悬停提示补充解释该功能。提示文案必须使用普通用户能够理解的产品语言，只说明用途、影响或使用方式，不得暴露技术术语、内部原理、协议、实现细节或调试信息。关键状态、错误、风险和完成任务所必需的操作说明仍须直接显示，不能只依赖悬停提示。
- 界面默认只保留帮助用户判断和操作所必需的短文案；可由悬停提示承载的补充说明不应再以大段常驻文字重复展示。需要持续可见的说明应拆短、分层或放到最相关的控件附近，避免说明文字挤占主要操作空间。
- 控件应尽量紧凑并按内容确定尺寸，避免没有必要的长条形组件，尤其避免横向铺满页面的按钮、开关容器、选择项和状态条。只有文本输入、进度、数据展示或布局确实需要连续宽度时才使用长条布局，并确保视觉层级清晰。
- 设计必须统一考虑整体美观与视觉秩序：同层级元素的边缘或文字基线应对齐，同类控件的尺寸应一致，页面边距、分区间距、控件间距和内部留白应遵循统一节奏；不得出现无理由的尺寸、对齐或间距差异。
- 中文界面文字的最终显示字号不得小于 12pt。不得使用 8～11pt 的中文文字，也不得通过 `minimumScaleFactor` 把中文缩小到 12pt 以下；空间不足时优先增加控件高度、调整布局、允许换行或对次要内容做尾部截断。
- 尽量不使用下拉列表，尤其避免把基础按键、系统动作、自定义动作和具体 APP 混在同一个长列表中。选项较多时必须按语义分区，优先使用页面内按钮网格、分段选择或清晰的分组列表。
- 能在页面内铺平完成的流程尽量不使用 Popover、Sheet 或连续确认弹窗。相关配置应集中到同一个大页面中，让用户看到当前对象、可选动作、附加设置、学习状态和测试入口，并一次性完成操作。
- 仅在系统文件选择、必要权限授权和不可恢复的危险操作中保留系统弹窗；普通说明、学习进度、成功和失败反馈应优先在当前页面内显示。

## 代码对应位置

- 窗口创建与最小尺寸：`Sources/RemoteMic/RemoteMicApp.swift`；
- Onboarding 根视图、布局和门禁：`Sources/RemoteMic/RemoteMicRootView.swift`、`Sources/RemoteMic/OnboardingView.swift`、`Sources/RemoteMic/OnboardingFlow.swift`；
- 页面布局、材质和遥控器热点：`Sources/RemoteMic/SettingsView.swift`；“回眸”内容布局与 App 图标：`Sources/RemoteMic/TranscriptHistorySection.swift`；
- 实体按键活动状态：`Sources/RemoteMic/HIDRemoteMonitor.swift` 与 `Sources/RemoteMic/BridgeAppModel.swift`。

## 结论

当前仓库截图对应 macOS 26 Liquid Glass 外观；同一页面结构在 macOS 14/15 自动切换到兼容样式。仓库未保留依赖本机临时目录的审查引用。

首次 Onboarding 已通过编译、自动化和静态字号检查。锁屏状态下已直接实例化生产 `OnboardingView`，通过离屏 AppKit 窗口缓存并逐页检查实体遥控器浅色/深色各 9 张、iPhone 和网页版浅色/深色各 10 张，共 58 张真实实现截图；另检查实体遥控器误按语音键纠正状态的浅色/深色截图。音频页只展示 MiRemoteV 2ch 与 BlackHole 2ch。`1020 × 772` 内容区、原生窗口框架、底部导航、纠正状态卡和右侧实时检查均无裁切。右侧插画与检查区跟随系统外观：浅色使用浅蓝画布，深色使用深蓝画布，左右区域不会形成黑白分栏。真实环境验收仍按 `Testing/FirstRunOnboarding.md` 执行，不能用截图替代 RC003、iPhone Nearby、手机 Safari 网页版、系统权限、音频回环设备和第三方语音工具验证。

2026-08-11 全流程恢复审计补充了遥控器 HID 状态与“重新检测按键”、音频页回到前台自动刷新，以及完成页对当前权限、BLE 和音频的最终重验。当时的浅色、深色各 8 张生产 Onboarding 截图已逐张检查；设备卡新电池图标仍需在浅色、深色生产设置页和真实充电线状态下复验。

## 2026-08-16 组合动作页面 QA

- 参考：用户提供的新建前后、组合动作编辑及遥控器触发方式截图；实现：`1.8.24 (117)` 本地 ad-hoc 构建，窗口内容约 `1024 × 768`。
- 已在同一视觉对比中检查参考与实际页面：页面信息层级保持一致，遥控器固定尺寸，新建前后不会缩放；名称标签和输入框同行；步骤编号与动作标题字号、字重一致；App 图标、名称和 Bundle ID 位于同一信息行。
- “测试动作”与可用状态的“保存修改”使用主要按钮样式；失焦或禁用时按照 macOS 系统规则降低强调，不额外伪造蓝色。
- 遥控器触发方式文字会根据按键位置做纵向动画：左键位于右上区域，TV 键位于右下区域；点击页面热点和真实 HID 按键都更新当前按键。
- 已安装 App 网格保持收起式展示；当前机器实际识别无线麦、Codex、Claude、cmux、飞书、微信、天气、豆包、OpenCode、剪映、Cursor、Xcode、网易云音乐、Chrome、Safari 和 Zed。WorkBuddy 按 `com.workbuddy.workbuddy` 过滤，需在安装该 App 的用户机器复验。
- 实测名称超过 20 字符会截断，回车保存后焦点退出输入框；列表删除辅助操作可删除临时组合动作并显示成功状态。
- final result: passed。真实遥控器触发提示和 WorkBuddy 显示仍列入用户机器人工验收，不以当前机器截图替代。

2026-08-17 语音工具页移除左栏内部滚动，四个工具卡统一高度并顶部对齐；豆包/微信四项输入法设置迁移到右栏。新增两层控制方式选择后，实体遥控器浅色/深色各 9 页、iPhone 和网页版浅色/深色各 10 页，以及豆包四种右栏指引状态和微信浅色/深色语音工具页均已用生产视图复验，在 `1020 × 772` 下无内部滚动、错位或裁切。

2026-08-17 控制选择改为两层：第一层并列显示“有遥控器 / 当前没有遥控器”并优先推荐实体遥控器；只有选择没有遥控器，第二层才显示推荐 iPhone App 与“没有 iPhone 时使用网页版”。生产状态已经确认遥控器连接时自动跳过第一层，但不会为判断提前启动 discovery。三种控制方式统一显示蓝牙、输入监控和辅助功能权限，连接页分别显示实体遥控器、Nearby 或二维码/WSS 状态。

2026-08-17 系统 Fn 第 3 项加入 macOS 键盘设置浅色/深色原图。生产视图已检查浅色、深色正常状态及浅色冲突状态，截图、实时说明、紧凑“打开键盘设置”按钮和底部导航均同时可见，未重新引入内部滚动。

## 2026-08-18 回眸页面验证

- 确认稿：`dist/designs/reflections-layout/expanded-app-switcher-collapsible-timeline.png`；实际实现截图：`dist/designs/reflections-layout/implemented-expanded.png`；并排比较图：`dist/designs/reflections-layout/reference-vs-implemented.png`。
- 在生产设置窗口中确认“回眸”紧跟“统计”，页头隐私说明与最右侧开关完整可见；顶部应用栏可在单行和页面内网格之间展开、收起，“全部应用”始终可见。
- 使用当前真实的 2 个 App、1 个日期、12 条记录确认单 App 筛选、倒序时间线、每条记录的 App 图标和名称、日期折叠恢复，以及页面底部无包围样式的“全部删除”；验证期间未执行任何删除。
- 生产窗口当前最小尺寸高于 `800 × 650`，本轮按实际生产窗口完成检查，不能表述为已在 `800 × 650` 生产窗口验收。10 App 横向滚动、跨日期默认折叠和浅色/深色仍需对应数据与外观环境复验。

final result: passed（上述真实数据与窗口边界除外）

## 2026-08-18 本地 Agent 访问区验证

- 在当前生产最小窗口中确认本地 Agent 访问区位于完整历史之后、“全部删除”之前；历史仍是页面主内容，新增区域没有制造核心空白或遮挡记录。
- 缺少设置事件时自动化确认默认关闭；实际页面检查时“允许访问”保持关闭，关闭状态仍显示授权列表，没有创建授权、复制令牌或改动历史。
- 中文标题、说明、开关、授权列表和底部操作均完整显示，字号不低于 12pt；页面内没有新增下拉框、Popover 或 Sheet。
- 当前生产窗口最小尺寸高于 `800 × 650`，本轮沿用实际生产最小窗口验证；开启、生成配置、长客户端名称、英文、浅色/深色和真实 MCP 客户端仍按测试手册人工验收。

final result: passed（上述人工状态与窗口边界除外）

## 2026-08-19 组合动作与回眸 MCP 边界说明

- 组合动作页底部增加固定 12pt 中英文信息行，明确“学习输入框”只负责聚焦目标 App，“回眸”中的 Codex MCP / TOML 只负责授权读取本地历史；没有新增副标题、下拉框、Popover、Sheet 或确认弹窗。
- 宿主公开构建和注入最新私有模块的编译、本地化及设置页结构测试通过；组合动作现已无需邀请码直接显示，本次仍未完成中英文、浅色/深色和 `800 × 650` 真实组合动作页面人工验收。

## 2026-08-20 自定义快捷键选择器 QA

- 自定义快捷键编辑区新增页面内“常用组合 / 标准键盘”分段选择，不使用下拉列表、Popover 或 Sheet；原有键盘录入、当前值和清除入口仍位于同一编辑区。
- 常用组合在 `800 × 650` 中文浅色生产 App 视图中完整显示；选择态使用低透明度语义蓝，选择预设本身不会执行快捷键。
- 标准键盘在 `800 × 650` 中文浅色、中文深色和英文浅色生产 App 视图中检查了组合键、主键区和方向导航区；Command 等长标签保持单行，键盘没有横向滚动或横向裁切。
- 同一生产视图以 `800 × 1400` 展开检查了 F1～F20、数字键盘和单独左/右 Command、Option、Control、Shift 与 Fn；全部中文说明和按键文字不小于 12pt。最小高度下通过既有页面纵向滚动访问后续键区，不改变窗口几何。
- 自动化覆盖预设键码、标准键盘关键分区、左右 Option 键码和单独修饰键的按下/释放顺序。离屏生产视图与自动化不能替代实体遥控器、真实 Spotlight、系统权限和左右修饰键目标 APP 的桌面验收，人工步骤见 `Testing/KeyboardShortcutPicker.md`。

final result: passed（真实遥控器与真实系统快捷键验收除外）

## 2026-09-05 Onboarding 语音配置核对卡 QA

- 生产 `OnboardingView` 在语音测试输入框页增加配置核对卡，显示无线麦发送键、SayAll 音频输出以及第三方工具必须自行设置的语音识别键和麦克风；豆包额外显示全局唤起语音。
- 微信输入法、Typeless、其他语音工具分别检查了长按 Fn、点按 Fn 和“需支持语音输入并自行配置麦克风”的差异文案；切换工具或音频设备后用户确认会清除。
- 语音测试页浅色/深色以及实体遥控器、iPhone、网页版控制方式截图均无明显裁切；音频设备未就绪显示红色，不能误导为已通过。
- 真实第三方 App 内部按键和麦克风状态仍不可观察；截图、自动化和离屏渲染不能替代真实豆包/微信/Typeless 文字上屏验收。

## 2026-08-24 语音键模式布局 QA

- “按键映射”页底部的 HID 状态、选择锁定、语音触发键、Fn 点按、短按聚焦和恢复默认使用稳定纵向分区；三段选择和两个开关保留在页面内，不使用下拉列表、Popover 或 Sheet。
- 使用生产 SwiftUI 视图和中英文正式资源完成 `800 × 1400` 压力渲染，并用英文完成 `1020 × 1400` 渲染；侧边栏、左右映射卡、三段选择、Fn 点按区和恢复默认按钮均无横向裁切。
- `800` 宽结果只属于截图 harness 压力审计，不代表生产设置窗口支持 `800 × 650`；真实窗口默认及最小尺寸仍为 `1020 × 772`。
- 新增和既有中文控件保持 12pt 或更大；帮助文字允许换行，没有使用 `minimumScaleFactor` 缩小中文。
- 自动化已增加 footer 紧凑布局门禁。真实 `1020 × 772` 窗口逐一点击侧边栏、纵向滚动、中英文、浅色/深色和系统增强对比度仍按 `Testing/VoiceKeyModes.md` 人工验收。

final result: passed（横向布局与静态字号；真实窗口交互和真实设备除外）

### 快速连接补充约束

- Codex、Claude Code、Cursor、OpenCode 使用页面内两列卡片，不使用下拉列表、Popover 或 Sheet；每张卡显示客户端图标或回退图标、名称、检测/连接状态和单一主操作。
- 手动客户端名称与复制配置保留在快速连接卡片之后，作为冲突、JSONC、CLI 失败和其他 MCP 客户端的降级路径。
- 自动连接失败只显示页面内错误文案，不连续弹窗；在 `800 × 650` 检查中必须确认四张卡、手动配置区、授权列表和页面底部“全部删除”可通过现有页面滚动完整到达。
- “已授权客户端”只展示当前有效授权，不展示快速连接失败产生的撤销记录；错误文案必须包含对应客户端名称和可行动的失败原因，不能只显示统一的“自动连接失败”。
- 快速连接成功状态必须显示具体客户端名称，例如“请重新打开 Codex”；说明文案明确无需重启无线麦，避免把“客户端”误解为当前 App。
- App 改名、移动或旧连接缺少 Helper 路径指纹时，客户端卡和授权行使用橙色文字显示“先移除，再重新连接”；提示留在页面内、不弹窗，不把自定义安装路径或令牌展示给用户。

## 2026-09-10 设置页设计稿一致性 QA

- 视觉真值：`Screenshots/design-drafts/settings-page/option-1-revision-1.png`，PNG，`1442 × 1091`，SHA-256 `3aaaa7ff12f3fbb8a5546d5ac11477a60f11f32fb1c714a557367bd68293521f`。
- 实现截图：`Screenshots/settings-page/actual-settings-zh-hans-light.png`，生产 App 资源包的隐藏离屏入口，窗口 `800 × 650`、Retina 输出 `1600 × 1300`，中文浅色，SHA-256 `6192b69d53de67c3ec828029f021efad59a88f5a29fde7e5b5f1643676f1c04b`。
- 底部分享状态：`Screenshots/settings-page/actual-settings-share-bottom-zh-hans-light.png`，同窗口与密度，侧边栏分享入口触发展开并滚动到底部，SHA-256 `2a7205af09ab6d542817f363decf81875066414ba3a3a7a60d5181c69d7445cc`。
- 完整同图对照：`Screenshots/settings-page/qa-comparisons/design-vs-actual-zh-hans-light.png`，把设计稿等比缩放并居中到 `1600 × 1300` 后与实现并排，SHA-256 `d8b352f9a9db36e927939c1f7613287c2aa32a4e7a3240d532afa4bef9c6319f`。
- 顶部重点区域对照：`Screenshots/settings-page/qa-comparisons/design-vs-actual-top.png`，用于核对应用信息、版本检查、Release Notes、权限行和分隔线；没有对设计或实现内容做重绘，SHA-256 `974bb53aa7d328731e24d4f4a8db5f642a881c6a242d3b8a87145218272defeb`。

### 比较历史

- 第一轮实现（Commit `e83f9d4d`）存在 P1：首屏被大号品牌 Hero、反馈大卡和默认展开的分享二维码占据，版本、权限与通用无法形成设计稿的紧凑系统设置层级。
- 修正：移除顶部独立 Hero 与反馈大卡；把 App 信息合并到“应用与更新”；权限、通用、诊断改为无大圆角容器的平铺行和分隔线；反馈、官网与 GitHub 收敛为底部支持行；分享移动为内容栈最后一个模块，侧边栏入口通过稳定锚点定位并展开。
- 第二轮同图复核：上述 P1 已消除。实际产品保留既有多页面侧边栏、预发布更新开关和官网/GitHub 功能，因此不会复制设计稿仅有单个侧边栏入口的静态示意；这些差异属于现有功能边界，不改变主内容的信息架构和视觉语言。

### 必查视觉面

- 字体与排版：使用系统字体与语义字重；正文与辅助说明不低于 12pt；中英文均未使用 `minimumScaleFactor`；英文长按钮在窄窗口按系统规则截断，完整功能可访问。
- 间距与布局：主要分区、行高、图标列、状态列、操作列和分隔线已与设计稿的紧凑节奏一致；`800 × 650` 首屏可同时看到应用更新、完整权限和通用开头，纵向滚动访问其余内容。
- 颜色与视觉 token：使用系统背景、分隔线、语义蓝和权限绿色状态，浅色与深色均保持对比度；不再使用与设计稿不一致的大面积卡片和阴影层级。
- 图片质量：继续使用 App 包内真实应用图标，未替换为占位图、重绘图或代码近似资源；设计稿原图字节保持不变。
- 文案与内容：应用与更新、权限与隐私、通用、诊断与日志均与确认方案一致；待更新版本 Release Notes 位于检查更新区域右侧；导入/导出与两个启动开关各自保持同一行；分享为页面最后一个模块。

### 剩余边界

- 截图证明静态布局、语言和外观，不替代真实系统权限跳转、Sparkle 安装更新、文件导入导出、登录项审批和实体设备验收；对应人工步骤保留在 `Testing/AboutUpdateCenter.md`。
- 设计稿画布比例与生产 `800 × 650` 窗口比例不同；同图对照采用等比缩放和居中，不把画布留白或既有侧边栏页面数量作为实现缺陷。

final result: passed

## 2026-09-10 App 聚焦快捷键与 Agent 目标

- 生产设置视图在 `800 × 650` 下完成中文浅色、中文深色和英文浅色离屏截图；目标 App 网格、聚焦方式、当前 `⌘L`、录入/清除与标准键盘入口没有横向裁切。中文使用 12pt 或以上，没有下拉框或新增弹窗。
- `800 × 1700` 压力截图确认 Command/L 选择态、完整主键区、导航键、F13–F20、数字键盘、单独修饰键和“测试打开与聚焦”完整展示；650 高度通过现有页面滚动访问。
- 原始 PNG 保存在 `Screenshots/app-shortcuts/`：`zh-light.png`、`zh-dark.png`、`en-light.png`（800×650）与 `zh-full.png`（800×1700），未缩放或重绘。
- 截图 fixture 使用独立 UserDefaults，预置 Cursor/Codex 和 Cmd+L，不修改用户配置。所有侧边栏页面已执行生产离屏渲染；电脑锁屏，尚未完成真实窗口逐项点击与滚动、输入监控/辅助功能授权、实体键盘、遥控器和第三方输入框验收。截图不等于这些用例通过。

final result: passed（静态布局；真实交互验收边界见 `Testing/CustomApplicationFocus.md` 用例 2B）
