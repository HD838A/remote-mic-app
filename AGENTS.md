# 仓库开发规则

## TODO 同步

- 开发或完成新功能时，必须检查 `TODO.md` 是否存在对应条目。
- 如果存在对应条目，必须在同一次修改中同步更新其完成状态和必要说明。
- 只有功能实现并完成相应验证后，才能将条目标记为完成。

## iOS 遥控器布局约束

- iOS 中间六个按钮的固定顺序为：第一行“返回 / 菜单 / 音量+”，第二行“主页 / TV / 音量-”。
- 这是已经确认的产品布局；除非用户明确要求调整顺序，否则任何功能开发、修复或重构都不得改变该顺序。

## iOS 验证边界

- iOS 页面保持固定单屏且不支持滚动；涉及布局变化时，至少使用 iPhone 12 尺寸确认内容完整显示。
- 只按改动范围执行验证：纯 iOS UI 修改无需运行完整 Mac 测试；只有共享协议或 Mac 代码发生变化时才运行相关 Mac 测试。
- 相关页面与状态没有变化时，不重复生成或检查相同截图。
- 提交和 Push 前检查邮箱、账号、Token、私钥、证书、截图及其他个人信息泄露；`.codex-screenshots/` 不得进入提交。

## iOS TestFlight 发布边界

- 发布前更新版本号并运行 `xcodegen generate`，完成必要的本地构建验证后再提交。
- 已有修复与新功能应尽量分开 commit，但不得仅为拆分提交而反复改写已经完成的历史。
- 明确要求发布 TestFlight 时，Push `main` 后再 Push `release/testflight` 触发 Xcode Cloud。
- `release/testflight` Push 成功后即完成自动化交接：不等待、不轮询、不检查 Xcode Cloud、App Store Connect、TestFlight 处理状态或可安装状态，除非用户在当前请求中明确要求监控。
- 不主动重新发布 Mac 安装包，除非用户明确要求；共享协议新增字段必须保持可选并兼容旧版 Mac。
