# Issue #386：上一首/下一首使用系统媒体事件

## 复现与日志

原实现发送 `⌘←/⌘→`，依赖前台播放器解释快捷键；用户反馈播放器不在前台时无法切歌。

## 根因与修复

上一首、下一首和播放/暂停改用 macOS system-defined media key 事件；保留原有枚举 raw value 以兼容已保存配置。

## 验证边界

`RemoteButtonsTests` 和完整 Swift 测试通过；真实播放器、系统媒体会话和实体遥控器仍需实机验收。
