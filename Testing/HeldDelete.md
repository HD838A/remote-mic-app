# 长按持续退格

1. 把菜单键长按映射为 deleteBackward；在临时空白文档中输入测试文本，不选中文字。
2. 短按菜单：原单击动作保持，不删除。长按：550 ms 后首次删除，其后每 150 ms 删除一次。
3. 松开立即停止；再长按重新从 550 ms 门槛开始。
4. 按住期间切换 App、断连或停用映射：不得继续删除；恢复后必须重新按下。
5. 有选区时首次退格仍遵循当前 App 的选区删除语义。确认其它非删除长按仍只触发一次。

自动化：swift test --filter 'RemoteButtonsTests|RemoteButtonGesture'，121 项通过，包含回归红绿验证。实体测试待执行。
