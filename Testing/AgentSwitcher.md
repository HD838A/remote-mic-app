# Agent 切换面板验收

## 用户路径

在「按键」页选择遥控器的菜单键，将单击动作设为「自定义 → 切换 Agent」。在下方勾选需要的 App（默认 Cursor、Codex），可通过「添加 App」加入其他应用。旧按键映射不会自动改动。

按菜单键打开屏幕中央的 App 图标面板，左右键循环选择，OK 打开或切入选中的 App，返回或再次按菜单取消。打开应用后，面板确认目标已成为前台再关闭，然后执行该 App 已保存的聚焦方式（快捷键或已记录输入框）；明确选择「只打开 APP」时不聚焦。没有保存配置的 Codex、Claude 和 cmux 使用现有内置聚焦方式，其余 App 需要先配置。鼠标可直接点选，键盘支持左右、Return 和 Escape。「预览切换面板」供鼠标/键盘试用，不绑定某台遥控器。

未安装的目标不会进入运行面板；全部不可用时显示空状态。打开失败保留面板供重试或取消；30 秒无操作、设备断开、输入监听重置、休眠或退出时关闭。其他普通按键退出面板并继续原动作；语音按钮仍使用原来的即时按下/释放链路。

## 自动化覆盖

- `AgentSwitcherControllerTests`：环绕选择、确认后前台验证、失败与重试、空状态、取消、超时、过期回调及 owner 隔离。
- `AgentSwitcherInputRoutingTests`：真实 HID report parser 到模态路由；方向和 OK 不泄漏原映射，关闭后的释放不触发双击/长按，重复 report 不重复移动，开启时清除旧计时器，断连/权限失败恢复，原生 Command-Tab 修饰键释放。
- `AgentSwitcherSettingsTests`：默认 Cursor/Codex、显式空列表、去重与 32 项上限、持久化、旧格式导入和配置隔离。
- `AgentSwitcherFocusTests`：按所选 App 复用聚焦配置、内置方式回退、只打开、前台目标变化和日志边界；控制器测试覆盖面板关闭后仅交接一次、取消和迟到回调不聚焦。
- 原快捷键录入、App 聚焦、HID、Apple Remote、移动端和语音测试仍作为回归集执行。

## 本机与真实设备步骤

1. 在当前构建中绑定菜单键，选择 Cursor 和 Codex。菜单 → 右 → OK 应只切入 Codex；菜单 → 左应从首项环绕至末项。
2. 连续开启/取消两次，再使用键盘左右与 Escape，确认面板重复打开后的监听正常。
3. 按住方向、OK、菜单，确认一次物理按下只生效一次；给 OK 配置双击/长按动作后再确认，不能触发这些原动作。
4. 用返回、菜单取消后按方向键，确认恢复原映射。语音按下和释放必须保持即时，不能因面板停留而延后。
5. 关闭所有 Agent、删除或移动一个所选测试 App，验证冷启动、不可用目标、空列表、失败重试。不得读取第三方 App 私有文件。
6. 面板打开时断开所属遥控器、关闭映射、撤销权限、休眠/唤醒，确认没有残留拦截。另一设备断开不能取消所属设备面板。
7. 检查中文浅/深色、英文，以及 2 个和超过 5 个目标。箭头选择滚动区域外目标时必须自动滚入视野。
8. 分别验证小米、Siri Remote 和移动端。手机与 Watch 目前沿用已有移动来源分组，不能据自动化 owner 测试宣称同时多移动设备完全隔离。
9. 在「打开自定义 APP」中为 Cursor 保存 ⌘L 聚焦方式，再切回菜单键的「切换 Agent」。将 Cursor 焦点放到编辑器，菜单 → 选择 Cursor → OK，确认聊天输入框获得焦点；保持聊天输入框可见后再次菜单 → 选择 Cursor → OK，确认仍在 Agent 输入框而不是切回编辑器。Codex 和 cmux 在无保存配置时分别验证输入框、当前终端焦点。不能只根据窗口置前或快捷键已提交判定通过。
10. 将测试 App 配置为「只打开 APP」，确认切换不额外发送快捷键；选中 App 后立刻切到另一 App、取消、重开面板或超时，不能让旧聚焦请求把输入发给另一 App。分别覆盖已运行、冷启动和权限缺失。

## 证据与边界

截图和自动化只证明对应 UI / 软件路径。真实蓝牙遥控器、系统权限切换、多屏/全屏、第三方 App 冷启动和输入焦点仍须单独记录，未实测项保持候选状态，PR 保持 Draft。此功能不选择单个 Agent 内的任务，也不提交输入内容。

日志使用 `AGENT SWITCHER` 的 presented/navigate/submitted/completed/attempt_failed/cancelled 阶段；同次会话带进程内 `operation_id`，重试带 `attempt_id`；配置日志仅记录数量。不得写入设备 owner、App 内容、路径或键入文本。

切换日志的 `result=frontmost` 仅表示应用置前。随后聚焦交接以 `switcher_operation_id` 关联切换，并使用独立聚焦 `operation_id`；`APP FOCUS submitted ... result=unknown` 仅表示快捷键已发出，最终焦点仍须观察目标 App。

## 2026-09-10 候选构建记录

- Swift 自动化：504 tests / 43 suites 通过；独立 selftests：44 通过。
- 本机使用生产设置页绑定菜单键为「切换 Agent」，Cursor/Codex 多选和预览正常。真实窗口中 Right 高亮 Codex，Return 确认后通过 NSWorkspace 前台检查关闭；Cursor 也有完成日志。再次预览后键盘仍可操作，Escape 取消正常。
- 实际运行截图位于 `Screenshots/agent-switcher/`，中文设置页与面板在浅/深色下检查。英文 980×732 截图单独标为 harness，不替代真实英文窗口验收。
- 开发包当时提示缺少输入监控权限。实体遥控器菜单 → 左右 → OK、撤销权限/蓝牙恢复、多屏/全屏和第三方冷启动尚未完成受控验收；不将键盘预览结果写成硬件通过。
- macOS 原深色外观已确认恢复。开发包为本机 ad-hoc 构建，不能当作公证安装包发布。
- 后续本机为开发版重新授予输入监控和辅助功能，日志确认两项权限及 HID filter ready。用户确认实体遥控器已可正常切换，同时报告输入框未聚焦；此反馈促成本轮聚焦交接修复，见 [Bug 记录](../Bugs/2026-09-10-agent-switcher-input-focus.md)。
- 聚焦修复源提交 `032da34a9df5385c1d02df93e37cd82546363c61`：完整测试 504 项 / 43 suites、自检 44 项、原生 Debug 构建通过；本机预览确认 Cursor 聊天输入框焦点，以及 Codex/cmux 既有聚焦路径成功日志。后续用户反馈暴露 Cursor `Cmd+L` 重复切换会回到编辑器，见 [Cursor 切换后焦点回到代码编辑器](../Bugs/2026-09-10-cursor-chat-focus-toggle.md)。新版本的实体遥控器聚焦、冷启动与剩余矩阵仍须单独验收。

## Cursor 重复聚焦回归

保存 ⌘L 时，切换器直接定位聊天框，不自动发送这个切换快捷键；识别失败或聊天区隐藏时保持当前界面并报告聚焦失败。连续切入至少三次，分别覆盖聊天框已有焦点、编辑区有焦点、聊天区隐藏和非空草稿；每次检查聊天输入区，不能把代码编辑器或搜索框当作成功。非空草稿不应被读取、改写或提交。其他自定义快捷键保持原有行为。

2026-09-10 本机与自动化证据及未覆盖项见 [Cursor 聚焦回归记录](../Bugs/2026-09-10-cursor-chat-focus-toggle.md)。完整测试更新为 509 tests / 44 suites，selftests 44 项。
