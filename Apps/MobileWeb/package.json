{
  "name": "remote-mic-mobile-web",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "protocol",
    "relay",
    "web"
  ],
  "scripts": {
    "build": "npm run build -w @remote-mic/mobile-web-protocol && npm run build -w @remote-mic/mobile-web-web && npm run build -w @remote-mic/mobile-web-relay",
    "dev": "npm run dev -w @remote-mic/mobile-web-relay",
    "test": "npm run build -w @remote-mic/mobile-web-protocol && npm run test --workspaces --if-present",
    "typecheck": "npm run build -w @remote-mic/mobile-web-protocol && npm run typecheck -w @remote-mic/mobile-web-relay && npm run typecheck -w @remote-mic/mobile-web-web"
  },
  "engines": {
    "node": ">=20.19"
  }
}
